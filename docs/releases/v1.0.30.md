# Build 30

Original handoff date: 2026-09-22 10:50 PM EDT

## Player changes

- Reworded the Large MIDI warning in plain language and grouped its event and memory information clearly.
- Pauses the displayed loading timer while the warning waits for a decision, and removes that pause from the final elapsed time.
- Shows Per-note **Maximum rate** as gate frames per second and explains what one frame permits.
- Preserves event-level lag while using a monotonic resolved-timeline frontier for stable Per-note Effective speed.
- Keeps Effective speed moving through processed sparse/filtered passages but makes it stop behind a blocked output call.

## Project and distribution changes

- Changed current and future downloads to two loose, versioned executables plus `SHA256SUMS.txt`; no extraction or runtime sidecar is required.
- Added a complete public `CHANGELOG.md` and simplified the README, roadmap, user guide, technical reference, packaging instructions, and engineering backlog.
- Reviewed the tracked and live Release descriptions so application changes, repository work, verification, and historical provenance remain distinct.

## Verification

- Focused tests cover warning text/layout, elapsed-time pause accounting, frame-rate formatting/live updates, sparse/blocked gate progress, and the release allowlist.
- The complete deterministic suite passed 97/97 on x86 and 97/97 on x64.
- Both exact loose executables passed PE architecture, metadata, icon, no-sidecar, checksum, and bounded launch/normal-close checks.

## Known limitations

- Large-file memory figures remain estimates; allocation can still fail because of process state, fragmentation, system commit, or paging.
- Static Analysis does not model the live Per-note interval gate.

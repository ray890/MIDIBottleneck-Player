<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <runtime>
    <!-- x64 builds may allocate arrays larger than 2 GB.  The CLR still
         enforces its documented single-dimensional element-count limit. -->
    <gcAllowVeryLargeObjects enabled="true" />
  </runtime>
</configuration>
